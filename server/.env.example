PORT=3001

# Yelp API (yelp-fusion client uses https://api.yelp.com/v3)
YELP_CLIENT_ID=
YELP_API_KEY=

# When true or 1, remove businesses whose distance from the geocoded city center exceeds the search radius (Yelp may still return out-of-radius results).
STRICT_FILTERING=
